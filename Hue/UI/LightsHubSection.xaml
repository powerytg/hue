<UserControl
    x:Class="Hue.UI.LightsHubSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Hue.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:renderers="using:Hue.UI.Renderers"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <ListView x:Name="LightListView">
            <ListView.ItemTemplateSelector>
                <local:LightListTemplateSelector>
                    <!--Light-->
                    <local:LightListTemplateSelector.LightTemplate>
                        <DataTemplate>
                            <renderers:LightOverviewRenderer LightSource="{Binding}"/>
                        </DataTemplate>
                    </local:LightListTemplateSelector.LightTemplate>

                    <!--Refresh-->
                    <local:LightListTemplateSelector.RefreshLightListTemplate>
                        <DataTemplate>
                            <renderers:RefreshLightListRenderer/>
                        </DataTemplate>
                    </local:LightListTemplateSelector.RefreshLightListTemplate>
                </local:LightListTemplateSelector>                
            </ListView.ItemTemplateSelector>
        </ListView>
    </Grid>
</UserControl>
